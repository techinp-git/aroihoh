# AroiHoh (Brand.Delivery)

ระบบสั่งอาหารผ่าน LINE OA + LIFF (สั่งตรง ไม่เสีย GP) — เฟส A ใช้กับแบรนด์ตัวเอง "ชิมชีวา One Price 60" ย่านอโศก/มศว

## สถานะ ณ 24 ก.ค. 2026 (อัปเดตทุกครั้งที่ audit)
- EP-12 (หลายแบรนด์ ครัวเดียว) + EP-13 (SaaS readiness) = **done ครบทุก subtask**
- ✅ **`SETUP-1` ไม่ใช่คอขวดแล้ว** — แบรนด์ชิมชีวาบน prod มี channelId/secret/token/liffId ครบ และ webhook รับ event จริงจาก LINE (log: `signature OK → dispatch events`) · ตรวจสถานะจริงได้จาก `SELECT name,"lineChannelId","liffId" FROM brands` บน prod ไม่ใช่จากไฟล์นี้
- ✅ **LIFF เปิดใช้ได้จริงแล้ว** (9 ก.ย. 2026) — CD รอบ GHCR ฝัง `VITE_LIFF_ID` ลง bundle แล้ว (ยืนยัน `grep` เจอ LIFF ID ใน `/var/www/aroihoh-liff/assets/*.js`) · endpoint `aroihoh-order.jivecode.click` เสิร์ฟ 200 · ค่าจริง prod: **brandId `b6ce3905-8c3a-42cb-ae76-02e3f3a4c54f`** / **liffId `2010670184-iFdmCQYB`** · ลิงก์ลูกค้า `https://liff.line.me/2010670184-iFdmCQYB` (deep link `?view=points`/`?view=profile`/`?e=<code>`/`?orderId=<id>`) · **VITE_LIFF_ID เป็น GitHub *variable* แล้ว** ฝังอัตโนมัติทุก build (ไม่ต้องจำ export เอง) · ⚠️ เหลือสิ่งเดียวที่อยู่นอก repo: **LIFF Endpoint URL ใน LINE Developers Console** ต้อง = `https://aroihoh-order.jivecode.click/?brandId=b6ce3905-8c3a-42cb-ae76-02e3f3a4c54f` (ตรวจจาก server ไม่ได้) · US-52 สแกน QR จากกล้อง LINE จริงบนมือถือ = พร้อมทดสอบแล้ว (เดิมติดเพราะยังไม่มี LIFF ID บน prod)
- ✅ ปิดไปแล้ว 23 ก.ค.: US-08 (Flex) · US-09 (BullMQ queue) · US-19 (margin/breakeven) — ทั้งหมดทดสอบกับ Redis + DB จริงแล้ว
- ✅ US-10 Rich Menu ปิดแล้ว → **หนี้โค้ดฝั่ง LINE หมด** ที่เหลือรอ keys/deploy ล้วน ๆ
- ✅ DEPLOY-1 **เสร็จแล้ว** 23 ก.ค.: prod รันที่ `49.13.57.24` (`/opt/aroihoh`, compose: api+postgis+redis) · `aroihoh-api/order/admin.jivecode.click` HTTPS ผ่าน Caddy บน host (Caddyfile ร่วมกับ health/jcorp/router — แก้ต้อง `caddy validate` ก่อน reload เสมอ) · seed แล้ว · คิว BullMQ ต่อ Redis ได้จริง · dev-login ปิด (403)
- ✅ **CD อัตโนมัติ (GHCR image + pull)** 8 ก.ย. — **ทดสอบ deploy จริงผ่านครบ (public HTTPS เขียวทั้ง api/order/admin)**: push `main` → CI ผ่าน → `.github/workflows/deploy.yml` (trigger `workflow_run`) build+push 2 image ขึ้น GHCR — `ghcr.io/techinp-git/aroihoh-api` + `aroihoh-static` (liff+admin ฝัง `VITE_*` ตอน build, busybox image) → SSH เข้า VPS รัน `scripts/deploy.sh` (pull → `docker cp` static ไป `/var/www` → `up -d` **ไม่ build บนเครื่อง** → `prisma migrate deploy` → health check → prune) · api container ใช้ `image: ghcr.io/.../aroihoh-api:${AROIHOH_TAG:-latest}` (compose คง `build:` ไว้ fallback) · **rollback**: `bash scripts/deploy.sh <sha เก่า>` (image tag ด้วย sha ค้างบน GHCR) · seed **ไม่อยู่ใน** deploy.sh (กันทับข้อมูล) · GitHub secrets: `SSH_HOST/USER/KEY` + `CR_PAT`(read:packages ให้ VPS login GHCR) · variables: `VITE_API_BASE_URL`(ต้องมี `/api`) + `VITE_LIFF_ID` · คู่มือตั้ง secrets: `docs/cd-secrets-checklist.md` · ⚠️ **`git pull` บน VPS ล้มถ้ามีไฟล์ tracked ถูกแก้ค้าง** (เคยเจอ `package-lock.json` เพี้ยนจาก manual deploy เก่า) → `git checkout -- <file>` ก่อน · ⚠️ static image คนละ dockerignore (`deploy/static.Dockerfile.dockerignore` — BuildKit ใช้แทน root เพื่อให้ apps/liff/admin เข้า context)
- ⚠️ deploy gotchas ที่เจอจริง: seed ในคอนเทนเนอร์ต้อง `npx ts-node -O '{\"module\":\"commonjs\"}' prisma/seed.ts` (ESM พัง) · `VITE_API_BASE_URL` **ต้องมี `/api` ต่อท้าย** · เขียน log ที่ `/tmp` ไม่ได้ (มีของ project อื่น) ใช้ `/root/`
- ⚠️ legal blocker: **PDPA pack ร่างครบแล้ว** (9 ก.ย. 2026) อยู่ใน `docs/pdpa/` — privacy-policy · consent-notices · data-map · data-subject-requests · README (เช็คลิสต์ + ข้อที่ต้องเคาะ) · **ยังเปิดใช้จริงไม่ได้จนกว่าจะ** เติมข้อมูลกิจการ + ให้นักกฎหมายตรวจ + เคาะ 2 ข้อ: (1) การตลาดเปลี่ยนเป็น opt-in ไหม (ตอนนี้ `marketingOptedOut` default=false = ส่งได้ทันที ซึ่งไม่ตรงมาตรา 19) (2) ระยะเวลาเก็บข้อมูล (ยังไม่มี job ลบอัตโนมัติ)
- ⚠️ PDPA ที่โค้ดยังไม่มี: แถบแจ้งครั้งแรกใน LIFF · บันทึกหลักฐานความยินยอม · ต่อท้าย broadcast ด้วยวิธีปฏิเสธ · ดักคำ "หยุดข่าวสาร" ใน webhook · ปุ่มขอลบบัญชี (ตอนนี้ลบด้วย SQL มือตาม `docs/pdpa/data-subject-requests.md` — **ทดสอบแล้วว่าใช้ได้ ไม่ทำแอปพัง และลูกค้ากลับมาสมัครใหม่ได้**)
- ⚠️ ข้อเท็จจริงที่ต้องแจ้งลูกค้าและมักถูกลืม: เซิร์ฟเวอร์ Hetzner อยู่ **นอกประเทศไทย** (โอนข้อมูลข้ามประเทศ) · คำค้นที่ลูกค้าพิมพ์ในช่องหาที่อยู่ถูกส่งไป **OpenStreetMap** · ลบข้อมูลแล้วยังค้างใน backup อีก **14 วัน**
- ✅ ops (25 ก.ค.): prod มี **backup อัตโนมัติแล้ว** (`scripts/backup.sh` — DB+รูปแชต volume, cron ตี 3, retention 14 วัน) · **ENCRYPTION_KEY rotate เป็น random 64-hex จริงแล้ว** (เดิมเป็นสตริง comment ที่อยู่ใน repo) ด้วย `prisma/rotate-encryption-key.ts` — ห้ามแก้คีย์ตรง ๆ ต้องใช้ script (ดู deploy-hetzner.md)

## Source of truth
- Backlog/task: AI Intranet → project `fb-project` (org `c46e73f8-0328-4627-bb51-df0eb4b70c47`)
- เอกสารแผน: Knowledge Board "F&B" (`1f9d454c-1468-49bd-9311-357f73528bf1`) — การ์ด "แผนระบบ LINE OA + LIFF" (v2.0) และ "หลายแบรนด์ ครัวเดียว"
- อัปเดตความคืบหน้ากลับเข้า task ด้วย `post_task_log` ทุกครั้งที่จบงานสำคัญ

## Architecture (monorepo — npm workspaces)
| path | คือ | stack |
|---|---|---|
| `apps/api` | Backend API + LINE webhook | NestJS 11, Prisma, PostgreSQL+PostGIS, Redis/BullMQ |
| `apps/liff` | หน้าเว็บลูกค้าใน LINE — 3 แท็บ: เมนู (เมนู→ตะกร้า→เช็คเขต→สั่ง COD→ติดตาม) · แต้ม (US-52 สแกน `?e=` รับแต้ม + ประวัติ · US-53 แลกรางวัล → คูปอง QR) · โปรไฟล์ (สมุดที่อยู่ US-59); auth: dev-login (dev) / liff→/auth/line | React 18 + Vite + @line/liff |
| `apps/admin` | หลังบ้านร้าน (ออเดอร์/เมนู/รายงาน) | React 18 + Vite |
| `packages/shared` | types/constants ใช้ร่วม | TypeScript |

Module ใน `apps/api/src/modules/`:
- ลงแล้ว: `auth` (US-01 LIFF ID token→JWT), `menu` (US-14 CRUD + public list), `delivery` (US-15 zone check radius + fee flat/tiered/per_km), `orders` (US-02/04/05 สร้างออเดอร์ idempotent + re-check เขต/ราคา server-side + ดูออเดอร์ตัวเอง; US-12 admin เปลี่ยนสถานะไล่ลำดับ + audit log + list), `admin-auth` (US-29 login email/password→admin JWT + me), `admin-users` (US-30 CRUD user + RBAC role/สิทธิ์ต่อแบรนด์), `brands`, `health`
- pure domain (unit-tested — รวมทั้งโปรเจกต์ **242 tests / 23 suites**): `delivery/geo.ts`, `delivery/fee.ts`, `orders/pricing.ts`, `orders/status.ts`; orders.service มี unit test mock prisma/delivery
- ลงเพิ่ม: `payments` (US-07 COD mark-paid + config codEnabled ต่อแบรนด์), `reports` (US-13 backend — GET /admin/reports/daily, pure `summarizeOrders`), `customers` (US-35 admin list+detail+ประวัติออเดอร์, PDPA ไม่คืน phoneEnc, pure `computeCustomerStats`)
- ลงเพิ่ม: `profile` (US-58 โปรไฟล์ + สมุดที่อยู่ลูกค้า — ดูหัวข้อ US-58 ด้านล่าง)
- ลงเพิ่ม: `customers` (US-35), `chat` (US-21 core — chat_messages, conversations/thread/send; ยิงเข้า LINE จริงรอ SETUP-1)
- US-35 หน้าลูกค้า: ลิสต์โชว์ avatar + คอลัมน์แท็ก (TagPill สีตามชื่อ) + ป้าย 🚫 opt-out · แถบกรองแท็ก (chips + จำนวน, client-side filter) · `GET /admin/customers/tags` คืนแท็กที่ใช้จริง + จำนวน (`tagCounts` นับใน memory เพราะ Prisma group by array element ไม่ได้) — ใช้ทั้งตัวกรองหน้าลูกค้า **และ** tag picker ใน rule builder ของ broadcast (US-18 audience: เลือกแท็กเป็น chips คลิก toggle ได้ ไม่ต้องพิมพ์ comma) · `components/Avatar.tsx` (แชร์ระหว่าง Chat/Customers, fallback วงกลมตัวอักษร) + `nameHue`
- ลงเพิ่ม: `broadcasts` (US-18 — preview reach/create+list/detail · จอง message_logs(queued, dedupeKey) กันส่งซ้ำ · owner/manager · ยิง LINE จริงรอ SETUP-1 = BullMQ worker) · รับได้ทั้ง contentId+audienceId หรือ message+segment(tags) ad-hoc · customers PATCH /:id/opt-out (PDPA)
- ลงเพิ่ม: `content` (US-18 คลังข้อความใช้ซ้ำ CRUD) + `audiences` (US-18 กลุ่มเป้าหมายบันทึกไว้ · pure `rules.ts` resolveAudienceByRules ประเมินสด: tenure_min_days/order_count_in_window/lapsed/tags, match all|any, หัก opt-out เสมอ · presets 3 ตัว · CRUD+preview reach) — ทั้งคู่ owner/manager
- ลงเพิ่ม: `line` (US-10 core — POST /api/line/webhook/:brandId verify `x-line-signature` บน rawBody (main.ts rawBody:true, pure `line-signature.ts` 6 tests) · inbound text → Chat Center · follow → welcome · `LineClient` push/reply (gated: ไม่มี access token → skipped) · broadcast dispatch POST /admin/broadcasts/:id/dispatch drain message_logs) — ยิงจริงรอ SETUP-1 keys · Flex(US-08)/BullMQ queue(US-09)/Rich Menu ลงครบแล้ว (ดูด้านล่าง)
- ลงเพิ่ม: `notifications` (US-08/09 — คิวแจ้งเตือน LINE) · pure `dedupe.ts` (buildDedupeKey `confirm:<orderId>` / `status:<orderId>:<status>`, isRetryableStatus 429/5xx, backoffMs, isValidJob) · `notifications.service.ts` จองแถว `message_logs(queued)` ก่อนเข้าคิว → **P2002 = เคยส่งแล้ว ข้าม** (dedupe บังคับที่ DB unique ไม่ใช่แค่ระดับแอป) · BullMQ attempts 3 + exponential backoff, concurrency 3 · **ไม่มี `REDIS_URL` = โหมด inline** (ส่งทันที ไม่มี retry — dev/CI) · ไม่มี LINE keys = `unreserve()` ลบแถวที่จองทิ้ง ให้ส่งใหม่ได้เมื่อ keys มา (ตัวเลขไม่เพี้ยน) · hook ที่ orders.service: create → `notifyOrderConfirmed` (Flex), updateStatus → `notifyStatusChanged` (เฉพาะ `shouldNotify`) ทั้งคู่ `void ... .catch()` ไม่ให้ push พังทำ API พัง
- ลงเพิ่ม: `line/flex.ts` (US-08 — pure builder) `buildOrderConfirmFlex` bubble ใบยืนยัน (header สีแบรนด์จาก theme US-39 + รายการ + สรุปเงิน + ปุ่ม deep link `https://liff.line.me/<liffId>?orderId=`) · `orderConfirmAltText` (≤400 ตัวอักษร) · `statusUpdateText` · **`NOTIFY_STATUSES` = confirmed/delivering/completed/cancelled เท่านั้น** (pending/preparing/ready ไม่ยิง — ประหยัดโควตา) · `LineClient.pushFlex` · 22 tests
- ลงเพิ่ม: `line/richmenu.ts` (US-10 Rich Menu — pure builder, 24 tests) · กริด 3×2 = 6 ปุ่ม บนรูป 2500×1686 (`gridBounds` คอลัมน์ขวากิน 834 รับเศษ 2500÷3 ไม่ลงตัว — ไม่งั้นขอบขวากดไม่ได้) · **ไม่มี liffId = ตัดปุ่ม deep link ทิ้ง** เหลือปุ่มส่งข้อความให้ auto-reply ตอบ (กันปุ่มกดแล้วเงียบ) · `validateRichMenu` ตรวจก่อนยิง (LINE ตอบ 400 ไม่บอกเหตุผล) + `findOverlaps` · LineClient: createRichMenu / uploadRichMenuImage (**ใช้ `api-data.line.me` คนละโดเมน** · เช็ค jpeg|png ≤1MB) / setDefaultRichMenu / deleteRichMenu · API `GET /admin/line-config/richmenu/preview` (ไม่ยิง LINE) + `POST /admin/line-config/richmenu` (owner) · **rollback: อัปโหลด/ตั้ง default พังเมื่อไหร่ ลบเมนูที่เพิ่งสร้างทิ้ง** และลบตัวเก่า*หลัง*ตัวใหม่สำเร็จ (ลบก่อน = ลูกค้าไม่มีเมนูช่วงคาบเกี่ยว) · เก็บ `brands.richMenuId`
- ลงเพิ่ม: `loyalty` (US-50 แกนสะสมแต้ม EP-14 — ดูหัวข้อ US-50 ด้านล่าง)
- ยังไม่ลง: payment gateway US-06 (รอ SETUP-4), `telegram` (EP-11)
- ⚠️ **แต้มหมดอายุยังไม่ทำ** — ledger มี type `expire` รองรับไว้แล้ว แต่ยังไม่เคาะว่าจะให้หมดอายุไหม (ถ้าทำต้องมี job + แจ้งลูกค้า)
- migrations: 0001_init · 0002_add_cod_enabled · 0003_chat_messages · 0004_customer_tags · 0005_store_hours · 0006_broadcasts (customers.marketingOptedOut + broadcasts) · 0007_content_audiences (content_library + audiences + broadcasts.contentId/audienceId) · 0008_message_channel (MessageChannel reply|push + MessageType auto_reply/chat — groundwork ให้ US-09 ยังไม่ใช่ตัว US-09) · 0009_order_ready · 0010_roles_kitchen_chat · 0011_menu_cost (US-19: `menu_items.costPrice` + `order_items.unitCost` snapshot + `brands.fixedCostDaily`) · 0012_line_login_channel (`brands.lineLoginChannelId`) · 0013_chat_image (`chat_messages.imagePath`) · 0015_saved_addresses (US-58: `addresses.brandId` backfill + note/isSaved/isDefault/deletedAt/updatedAt) · 0016_loyalty (US-50: 5 ตาราง loyalty_* + `customers.pointsBalance`) · 0017_loyalty_guard (US-55: `brands.loyaltyDailyEarnCap`)
- US-03 เลือกจุดส่งบนแผนที่: `apps/liff/src/AddressPicker.tsx` — Leaflet + OSM tiles (ไม่ต้องมี API key) · หมุดลากได้/แตะแผนที่ปักได้ · ปุ่ม "ใช้ตำแหน่งปัจจุบัน" (geolocation — ปฏิเสธสิทธิ์ = บอกทางออก ไม่ใช่ error) · ค้นหาผ่าน **Nominatim ยิงตอนกดค้นหา/Enter เท่านั้น ห้ามยิงทุกตัวอักษร** (นโยบาย OSM) · ใช้ `L.divIcon` ไม่พึ่งไฟล์รูป marker (Vite หาไม่เจอ รูปแตก) · **ต้อง `invalidateSize()` ก่อน `fitBounds()`** และยิงซ้ำที่ 300ms + ResizeObserver ไม่งั้นกรอบใน LIFF ยืดไม่ทัน tile เหลือขอบเทา · public `GET /delivery/origin/:brandId` (ครัว lat/lng + รัศมี ให้วาดวงเขต — ไม่คืน fee rule) · ขยับหมุด → debounce 500ms → `/delivery/check` · **ค่าส่ง/ระยะยังคิดฝั่ง server เท่านั้น** วงกลมบนแผนที่เป็นภาพช่วยมองอย่างเดียว (#2/#5) · bundle LIFF โตจาก 85→130KB gz เพราะ Leaflet
- US-08 deep link: Flex ส่ง `?orderId=` มา → `DEEP_LINK_ORDER_ID` ใน `liff/src/api.ts` → boot เข้าหน้า track เลย (โหลดไม่ได้ = ตกไปหน้าเมนู ไม่ขึ้น error) · หน้า track มีปุ่ม "‹ กลับ" ไปสั่งเพิ่มได้
- US-18 broadcast: หน้า Compose กด "ส่ง" แล้ว **create → dispatch ต่อทันที** (เดิมกดแล้วแค่จองคิว ต้องไปกด "ส่งจริง" ในตารางอีกที = ผู้ใช้นึกว่าพัง) · dispatch ยังแยก endpoint เพราะวนยิงทีละคน หลุดกลางคันกด "ส่งจริง" ซ้ำได้ dedupeKey กันส่งซ้ำ
- US-16 store: kitchens.openTime/closeTime + isOpen · `store` module (GET /admin/store, PATCH pause/hours) · delivery.quote เช็ค isAccepting (พัก/นอกเวลา → order 422) · Settings มีการ์ดสถานะร้าน
- US-21 chat: 3-column (conversations | thread | customer panel = ประวัติออเดอร์+แท็ก) · tag ลูกค้า = customers.tags + PATCH /admin/customers/:id/tags · ที่เหลือ = LINE ส่ง/รับจริง (SETUP-1)
- แชตเก็บรูป (US-21): ลูกค้าส่งรูปผ่าน LINE → webhook `message.type==='image'` → `LineClient.getMessageContent` ดึง binary จาก **`api-data.line.me`** (คนละโดเมน) → `MediaService.save` เขียนลงดิสก์ → `chat_messages.imagePath` (migration 0013) · **ไม่เก็บ binary ใน DB** (บวม/backup ช้า) เก็บแค่ basename · รูปอยู่ใน **Docker volume `mediadata:/app/media`** (persist ข้าม rebuild) env `MEDIA_DIR` · เสิร์ฟผ่าน `GET /admin/chat/media/:messageId?token=` (auth ผ่าน token query เพราะ `<img>` ใส่ header ไม่ได้ — pattern เดียวกับ SSE) + `assertBrandAccess` กันดึงข้ามแบรนด์ + `safeMediaName` กัน path traversal · ดึงรูปพลาด = เก็บป้าย `[รูปภาพ]` ไม่ให้ห้องว่าง · sticker/location/ฯลฯ เก็บป้าย `inbound-preview.ts` · avatar โปรไฟล์ LINE (`customer.pictureUrl` มีอยู่แล้ว) โชว์ในลิสต์+header (โหลดพัง=วงกลมตัวอักษร) · **แท็ก manual มีอยู่แล้ว** (TagEditor + PATCH /admin/customers/:id/tags) · pure tests: media.filename (traversal), inbound-preview
- แชต auto-refresh: **poll 5s** (ยังไม่มี SSE เหมือน orders/KDS) ทั้ง inbox + ห้องที่เปิดอยู่ · หยุด poll เมื่อ `document.hidden` แล้วยิงทันทีตอนกลับมาที่แท็บ · `sameThread()` คงอ็อบเจกต์เดิมถ้าข้อความไม่เปลี่ยน (ไม่งั้น re-render ทุก 5s เด้ง scroll ลงล่างจนอ่านย้อนไม่ได้) · error ตอน poll เงียบ ไม่เด้ง alert รบกวนคนตอบแชต
- แชตเสียงเตือน (US-21): `lib/beep.ts` Web Audio (แชร์ Orders/KDS/Chat — dedup แล้ว) · beep เมื่อยอด **unread รวมเพิ่มขึ้น**ระหว่าง poll (ข้ามรอบแรก ไม่มีฐานเทียบ) · badge จำนวนบน **document.title** `(N) ...` เห็นแม้สลับแท็บ (คืนค่าตอนออกจากหน้า) · ปุ่ม 🔔/🔕 mute เก็บใน `localStorage['aroihoh.chatSound']` — กดเปิด beep สั้นปลุก AudioContext ด้วย user gesture · เปิดห้อง = mark read → unread ลด → badge/title หาย (ไม่ beep ตอนลด)
- admin routing: หน้าปัจจุบันอยู่ใน **URL hash** (`#/orders`) — refresh/แปะลิงก์/ปุ่ม back ใช้ได้ · แบรนด์ที่เลือกเก็บ `localStorage['aroihoh.brandId']` (ถ้าแบรนด์นั้นหายไปแล้วค่อย fallback ตัวแรก) · คลิกเมนู = push history, การเด้งอัตโนมัติตาม role (US-45) = replace (ไม่ให้ back ติดลูป)
- US-40 chat รวมทุกแบรนด์ (EP-12): GET /admin/chat/conversations ไม่ส่ง brandId = inbox รวมทุกแบรนด์ที่ admin มีสิทธิ์ (ส่ง brandId = กรองแบรนด์เดียว เดิม) · ทุกห้อง+thread ติด brandId/brandName · UI: BrandChip สีตามชื่อแบรนด์ + header "คุยผ่าน OA: X" + composer placeholder บอก OA + dropdown กรองแบรนด์ (โชว์เมื่อ >1 แบรนด์) · ตอบกลับ route ผ่าน OA ของแบรนด์นั้น (conv.brandId ไม่ใช่ตัวเลือกแบรนด์ด้านบน)
- US-11 realtime: SSE `GET /api/admin/orders/stream?brandId=&token=` (order-events.service RxJS bus, emit ตอน create/status) · admin Orders มี beep+flash+badge 🟢, EventSource reconnect · LIFF track poll 5s
- US-41 สถานะ ready (EP-12): order flow = pending→confirmed→preparing→**ready**(ครัวจัดเสร็จ รอไรเดอร์)→delivering→completed · migration 0009 (`ADD VALUE 'ready' BEFORE 'delivering'`) · ORDER_STATUS_FLOW + STATUS_TH (admin/liff) · status.ts generic ไม่ต้องแก้
- US-37 จอครัว KDS (EP-12): `GET /admin/kitchen/orders` (ไม่รับ brandId = รวมทุกแบรนด์ที่ admin มีสิทธิ์, active pending/confirmed/preparing/ready, เรียงเก่าสุดก่อน + brandName + customer.displayName + address) · SSE `GET /admin/kitchen/stream?token=` filter ตาม admin.brandIds · admin หน้า "ครัว (KDS)" การ์ด grid: BrandChip สีแบรนด์ + ขอบสีตามสถานะ + ปุ่มดันสถานะ (pending→รับออเดอร์ / confirmed→เริ่มทำ / preparing→จัดเสร็จ / ready→ไรเดอร์รับแล้ว) + beep/flash realtime · reuse changeStatus(brandId ต่อออเดอร์)
- US-42/43 พิมพ์ 80mm (EP-12): `apps/admin/src/lib/print.ts` (printHtml ผ่าน hidden iframe + kitchenTicketHtml/riderLabelHtml, @page 80mm) · กด "รับออเดอร์"(→confirmed) พิมพ์ใบครัว (เมนู/จำนวน/note ตัวใหญ่) · กด "จัดเสร็จ"(→ready) พิมพ์ label ไรเดอร์ (#order/แบรนด์/ชื่อ/ที่อยู่/พิกัด/ยอดเก็บ COD) · ปุ่ม 🖨️ บนการ์ด = พิมพ์ซ้ำ · KDS include customer+address (ไม่คืน phoneEnc PDPA) · พิมพ์เงียบ = Chrome `--kiosk-printing` (ดู docs/kitchen-print.md)
- US-36b คัดลอกเมนู (EP-12): `POST /admin/menu/copy {sourceBrandId,targetBrandId}` (assertBrandAccess ×2) copy หมวด+item ใน transaction (id ใหม่, คงราคา/isAvailable, append) · UI หน้า Menu การ์ด "คัดลอกเมนูจาก" เด่นเมื่อแบรนด์เป้าหมายว่าง
- US-44 จัดการครัว (EP-12): `kitchens` module (ย้ายจาก brands) — `GET /admin/kitchens` (รายละเอียด lat/lng/รัศมี/fee/brandCount, owner/manager), `POST`/`PATCH` (owner) · สร้างครัว provision flat DeliveryFeeRule ให้สั่งได้ทันที · แก้ flatFee = ปิดกฎเดิม+สร้าง flat ใหม่ · UI KitchenManager card ใน Settings
- US-38 dashboard รวม merchant (EP-12): `GET /admin/reports/merchant-daily` (ไม่รับ brandId, รวม admin.brandIds) = total + per-brand (count>0, เรียง revenue) · Dashboard section "รวมทุกแบรนด์" + ตาราง BrandChip (โชว์เมื่อ >1 แบรนด์มีออเดอร์)
- US-39 LIFF theme ต่อแบรนด์ (EP-12): `GET /brand/:brandId` (public) คืน name/logoUrl/theme · brand update รับ `theme` Json ({primaryColor}) · admin BrandManager ปุ่ม 🎨 ธีม (โลโก้ URL + color picker) · LIFF โหลด getBrand → หัวเว็บใช้ `--brand-primary` + โลโก้ + ชื่อแบรนด์ + document.title (ชิมชีวา=ส้ม default, A La Carte=เข้ม) · **EP-12 ครบทุก subtask**
- ⚠️ **LINE มี 2 channel คนละเลข ห้ามสับ** — Messaging API (`lineChannelId` + secret/token, ใช้ส่ง/รับข้อความ) กับ LINE Login (`lineLoginChannelId`, เจ้าของ LIFF app + **ผู้ออก ID token**) · `POST /auth/line` ต้อง verify ด้วยตัวหลัง ไม่งั้น LINE ตอบ 400 ลูกค้าล็อกอินไม่ผ่าน · pure `auth/login-channel.ts` `resolveLoginChannelId()` ไล่ลำดับ **ค่าที่กรอกเอง → เลขหน้า `liffId` (LIFF ID = `<loginChannelId>-<suffix>`) → `lineChannelId`** (backward compat) — ของเดิมที่ตั้งไว้แล้วจึงไม่ต้องกรอกเพิ่ม · 9 tests
- **หน้าตั้งค่า LINE OA** (US-25 บางส่วน/SETUP-1 ส่วน B): admin Settings การ์ด "เชื่อมต่อ LINE OA" (owner) → `line-config` module GET/PUT/POST test `/admin/line-config` · GET ไม่คืน secret/token (แค่ hasX + configured + webhookUrl) · owner เสียบ Channel ID/secret/token/LIFF ID เองได้ ไม่ต้องแก้ .env · `LineClient.getBotInfo` ทดสอบ token · env `PUBLIC_API_URL` ประกอบ webhook URL · encrypt secret/token แล้ว (ดู SEC-1 ด้านล่าง)
- **admin UI ครบ 9 หน้า** (feature-complete): Dashboard (สรุป+date picker เลือกวัน US-13), Orders (สถานะ/ยกเลิก/COD/realtime/⬇CSV export), Chat, **ส่งข่าวสาร/Broadcast** (US-18 — 3 แท็บ: ส่ง(เลือก content+audience+preview reach) / คลังข้อความ / กลุ่มเป้าหมาย(rule builder+presets), ซ่อนจาก staff), Menu (**CRUD เต็ม**: เพิ่ม/แก้/ลบ+หมวด+รูป US-14 · GET /admin/menu/categories), Customers, **สะสมแต้ม**(สแกนคูปอง staff+ / ล็อต QR + ของรางวัล + รายงาน owner-manager US-51/53/54/55), Users&RBAC(owner), Settings · US-13/14 done · **US-19 done** (CSV + margin/breakeven)
- **CI** `.github/workflows/ci.yml`: งาน `e2e` (postgis+redis→migrate→seed→boot→`apps/api/test/e2e.mjs` 197 เช็ค fetch ล้วน) ปิดหนี้ CTO · รัน local: `npm run test:e2e -w apps/api`

- US-45 roles kitchen/chat_agent (EP-13): AdminRole + kitchen (เห็นแค่ KDS+ไล่สถานะ) / chat_agent (เห็นแค่แชต) · migration 0010 · brand-scoped (staff/kitchen/chat_agent ต้องผูก brandIds, resolveBrandIds คืน assigned) · API gate: chat→+chat_agent, kitchen/admin-orders→+kitchen, customers(PII)/reports→ตัด kitchen/chat_agent (class @Roles) · admin NAV ต่อ role (roles[] ต่อเมนู + auto-redirect เมนูแรก) · Users dropdown + brand requirement · menu/store ยังเปิดทุก admin role (low-sev, documented)
- US-46 chat presence (EP-13): `chat-presence.service.ts` in-memory Map<customerId, viewers> TTL 12s · `POST /admin/chat/:customerId/presence` heartbeat คืน viewers คนอื่น · Chat.tsx heartbeat ทุก 8s ระหว่างเปิดห้อง → โชว์ "👁 X กำลังดูห้องนี้อยู่" ใน thread header (กันตอบชนกัน เบา ไม่ต้อง assignment)
- **US-59 LIFF โปรไฟล์ + แท็บล่าง** (EP-15, 8 ก.ย. 2026): LIFF เลิกเป็นเส้นตรง → มี **bottom nav** (`tab` = แท็บล่าง, `view` = หน้าย่อยในแท็บสั่งอาหาร) · **แท็บ "แต้ม" ผูกกับ EP-14 — โชว์เมื่อ `profile.loyalty` ไม่ใช่ null เท่านั้น** (ยังไม่ลง = เห็น 2 แท็บ ไม่ให้ลูกค้ากดเจอหน้าว่าง พอ US-50 ลงแท็บโผล่เอง ไม่ต้องแก้ LIFF) · deep link `?view=profile` / `?view=points` (Rich Menu US-10 ยิงตรงได้) · `ProfileTab.tsx` = avatar/ชื่อ/สมาชิกตั้งแต่ + การ์ดแต้ม + สมุดที่อยู่ + ออเดอร์ล่าสุด 3 (แตะแล้วเข้าหน้า track) · ฟอร์มหมุด reuse `AddressPicker` + ป้ายสำเร็จรูป 🏠 บ้าน / 🏢 ที่ทำงาน / ตั้งเอง
- US-59 เช็คเอาต์: ชิปเลือกหมุดจากสมุด (หมุดหลักถูกเลือกให้อัตโนมัติ, หมุดนอกเขต = ชิปเทา กดไม่ได้) + ชิป "📍 ปักหมุดใหม่" · **แตะแผนที่/แก้ที่อยู่ตอนเลือกหมุดจากสมุดอยู่ = หลุดจากสมุดทันที** (ใช้เฉพาะออเดอร์นี้ ไม่ทับหมุดในสมุด — มีข้อความบอก) · ติ๊ก "บันทึกที่อยู่นี้ไว้" ต้องเลือกป้ายด้วย ไม่งั้นครั้งหน้าชิปขึ้น "ที่อยู่" เหมือนกันหมดแยกไม่ออก
- US-59 บั๊กที่เจอตอนทดสอบจริงแล้วแก้แล้ว (อย่าทำซ้ำ): กดแท็บ "เมนู" หลังสั่งเสร็จแล้วค้างหน้า "สั่งสำเร็จ" ของออเดอร์เก่า → `goTab('order')` รีเซ็ต view เมื่ออยู่ done/track · **ตะกร้าไม่ถูกล้างหลังสั่ง** (ของเดิมค้าง กดสั่งซ้ำโดยไม่ตั้งใจ) → `setCart({})` หลัง create · โน้ตของหมุดในสมุดติดไปกับ "ปักหมุดใหม่" ทั้งที่หน้าเช็คเอาต์ไม่มีช่องแก้ → ล้าง note ตอนหลุดจากสมุด
- ⚠️ CSS LIFF: `.btn` และ `input` เป็น `width:100%` ทั้งคู่ — ใส่ใน flex row เมื่อไหร่ต้องล้มทั้งสองฝั่ง (`.rowbtns .btn`, `.chip-input`) ไม่งั้นโดนบีบจนหาย (แบบเดียวกับ `.picker-search`) · แถบล่างรวม `.bottom` (ปุ่มของหน้า + แท็บซ้อนกัน) ตรึงที่เดียว ไม่ต้องคำนวณ offset ให้ `.cartbar` เอง
- **US-60 ตั้งค่าโปรไฟล์ (PDPA self-service)** (EP-15, 8 ก.ย. 2026 — **EP-15 ครบทุก subtask**): LIFF โปรไฟล์มีการ์ด "ตั้งค่า" — เบอร์โทรให้ไรเดอร์ติดต่อ (normalize เป็น `0XXXXXXXXX` ก่อนเก็บ, **เข้ารหัสลง `phoneEnc`**, หน้าเว็บเห็นแค่ `••<4 ตัวท้าย>` + ปุ่มลบ) · สวิตช์ "รับข่าวสารและโปรโมชัน" = `marketingOptedOut` กลับด้าน ลูกค้ากดเองได้ · ลิงก์นโยบายความเป็นส่วนตัวจาก env **`VITE_PRIVACY_URL` — ไม่ตั้ง = ไม่โชว์ลิงก์** (PDPA pack ยังไม่มี ดีกว่าพาไป 404)
- US-60 หน้าลูกค้า (admin): แยก **"สมุดที่อยู่ของลูกค้า"** (ป้าย "บันทึกไว้" + โน้ต) ออกจาก **"ที่อยู่จากออเดอร์"** (snapshot — โชว์ 5 ล่าสุด + นับที่เหลือ) · ลูกค้าประจำมี snapshot สะสมเป็นสิบแถว เทกองรวมกันแล้วหาที่อยู่จริงไม่เจอ
- ⚠️ ทดสอบแล้วบน DB จริง: ตั้ง `ENCRYPTION_KEY` → `phoneEnc` เก็บเป็น `v1:iv:tag:ct` ส่วน API คืนแค่ 4 ตัวท้าย · **คีย์หาย = `hasPhone:false` เฉย ๆ ไม่ 500** (decryptSecret คืน null)
- **US-50 แกนสะสมแต้ม** (EP-14, 8 ก.ย. 2026 — backend ใช้ได้จริง, UI รอ US-51…55): module `loyalty` · migration **0016** (5 ตาราง + `customers.pointsBalance`) · แต้มแยกต่อแบรนด์ (ADR-07) · **แหล่งความจริง = `loyalty_transactions` (append-only) ส่วน `pointsBalance` เป็น cache ที่อัปเดตใน transaction เดียวกันเสมอ**
- US-50 จุดกันโกง (มี e2e คุมทุกข้อ): โค้ด QR ใช้ครั้งเดียวด้วย **conditional update `WHERE status='active'`** (ห้ามเปลี่ยนเป็น SELECT-แล้ว-UPDATE — สแกนพร้อมกัน 2 คนจะได้ทั้งคู่) · ล็อตเริ่มที่ **`draft` สแกนไม่ได้** จนกดเปิดใช้ (กันสแกนตั้งแต่โรงพิมพ์) · โค้ดข้ามแบรนด์/ล็อตปิด/หมดอายุ = **404 เหมือนกันหมด** ไม่บอกว่ามีอยู่จริง · ใช้แล้ว = 409 · **ตัดแต้มด้วย `WHERE pointsBalance >= cost`** → ติดลบไม่ได้ · ยืนยันคูปองซ้ำ = 409 + rollback (ไม่ตัดสองรอบ) · confirm เขียน `audit_logs` ทุกครั้ง
- US-50 API: ลูกค้า `POST /loyalty/earn` · `GET /loyalty/me` · `GET /loyalty/rewards` · `POST /loyalty/redemptions` (+`GET/cancel :id`) — **ออกคูปองยังไม่ตัดแต้ม ตัดตอน confirm ที่เดียว** จึงไม่ต้องมี job คืนแต้ม · pending ได้ทีละ 1 ใบ (ขอใหม่ = ใบเก่า cancelled) · แอดมิน `POST/GET /admin/loyalty/batches` (+`PATCH :id`, `GET :id/codes`) · `POST/GET /admin/loyalty/rewards` (owner/manager) · `GET/POST /admin/loyalty/redemptions/:token[/confirm]` (**staff ขึ้นไป** — คนขายต้องกดเองได้) · brandId ของ confirm มาจากตัวคูปอง ไม่รับจาก client
- US-50 pure `loyalty/ledger.ts` 20 tests: ชุดตัวอักษรโค้ด **ตัด 0/O/1/I/L ออก** (มีเส้นพิมพ์รหัสเอง) · `normalizeCode` รับขีด/ตัวเล็ก/ตัวที่พิมพ์สลับ · `formatCodeForHuman` กลุ่มละ 4 · คูปองอายุ 10 นาที · `nextReward` = รางวัลถูกสุดที่ยังแลกไม่ได้
- ⚠️ **ไม่ได้ใส่ CHECK constraint `pointsBalance >= 0`** — Prisma ไม่รู้จัก จะกลายเป็น drift ตอน `migrate dev` · ใช้ conditional update + e2e คุมแทน
- ✅ `/me/profile` คืน `loyalty` แล้ว → แท็บ "แต้ม" ใน LIFF ติดขึ้นมาเองตามที่ US-59 วางไว้ (ยืนยันในเบราว์เซอร์) — หน้าแลกรางวัล/คูปอง QR เต็มรูปแบบยังเป็น US-52/53
- **US-52 LIFF สแกนรับแต้ม + แท็บแต้ม** (EP-14, 8 ก.ย. 2026): deep link **`?e=<code>`** (pattern เดียวกับ `?orderId=` US-08) → boot → login → `POST /loyalty/earn` → หน้าผลลัพธ์ · **ลบ `?e=` ออกจาก URL ด้วย `history.replaceState` ทันที** ไม่งั้นรีเฟรชแล้วยิงซ้ำ เห็น "ใช้แล้ว" ทั้งที่เพิ่งได้แต้ม · หน้าผลแยก 3 เคสคนละข้อความ (409 ใช้แล้ว / 404 ใช้ไม่ได้ / error เชื่อมต่อ) เพราะทางออกของลูกค้าคนละเรื่อง · หน้าผลทับเนื้อหาแท็บทั้งหมดจนกดปุ่มออก (ปุ่มล่างของหน้าสั่งอาหารต้องไม่โผล่ด้วย)
- US-52 แท็บ "แต้ม" (`PointsTab.tsx`): ยอดแต้มใหญ่ + แถบคืบหน้าไปรางวัลถัดไป + ประวัติ (`GET /loyalty/me`) · โหลดใหม่ทุกครั้งที่เปิดแท็บ · โปรไฟล์ถูกโหลดก่อนรับแต้มในลำดับ boot → **ต้องดึง `/me/profile` ใหม่หลังสแกนสำเร็จ** ไม่งั้นการ์ดแต้มค้างยอดเก่า
- ⚠️ US-52 **ยังไม่ได้ทดสอบจากกล้อง LINE จริงบนมือถือ** (ต้องมี LIFF ID บน prod ก่อน) — ที่ทดสอบแล้วคือในเบราว์เซอร์ 4 เคส: สำเร็จ/สแกนซ้ำ/รหัสมั่ว/พิมพ์รหัสเองตัวเล็ก+มีขีด
- **US-53/54 แลกรางวัล + หน้าสแกนคนขาย** (EP-14, 8 ก.ย. 2026 — **วงจรแต้มครบแล้ว**): LIFF แท็บแต้มมีรายการของรางวัล (ปุ่มแลกเทาเมื่อแต้มไม่พอ **หรือมีคูปอง pending อยู่**) → `Coupon.tsx` QR (lib `qrcode` วาดลง canvas) + นับถอยหลัง + รหัสตัวอักษร + **poll 5 วิ → คนขายยืนยันแล้วจอลูกค้าเปลี่ยนเป็น "ใช้คูปองแล้ว" เอง** · admin หน้า **"สะสมแต้ม" (`views/Loyalty.tsx`, staff ขึ้นไป)** 2 แท็บ: สแกนแลกแต้ม + ของรางวัล (owner/manager)
- ⚠️ **`TOKEN_LENGTH` เปลี่ยน 24 → 12** ตอน US-54: รหัสที่คนขายพิมพ์เอง = token เดียวกับใน QR (ไม่มีสองค่าให้สับสน) โชว์เป็น `XXXX-XXXX-XXXX` · 59 bit + อายุ 10 นาที + ต้องล็อกอินแอดมินถึงยิงได้ · admin preview/confirm เรียก `normalizeCode` ก่อนค้น → พิมพ์ตัวเล็ก/มีขีดใช้ได้
- US-54 กล้อง: **`jsqr` + `getUserMedia` เอง ไม่ใช้ `html5-qrcode`** · **dynamic import ตอนกดเปิดกล้องเท่านั้น** (แยก chunk 47KB gz ไม่ถ่วงทุกหน้า — ถ้า import ตรง ๆ bundle admin โต 72→122KB gz) · ปิดกล้องเมื่อ unmount ไม่งั้นไฟกล้องค้างกินแบตมือถือคนขาย · **ช่องพิมพ์รหัสโชว์เสมอ** ไม่ซ่อนเป็น fallback — กล้องไม่ได้สิทธิ์/ไม่มีกล้องเกิดจริงหน้าร้าน (ทดสอบแล้ว NotAllowedError → ขึ้นข้อความบอกทางออก ไม่พังทั้งหน้า)
- **US-51 แผ่นพิมพ์สติกเกอร์ QR + CSV** (EP-14, 8 ก.ย. 2026 — **EP-14 เหลือแค่ US-55**): เคาะแล้วว่าใช้ **สติกเกอร์ใต้ฝากล่อง** · แท็บ "ล็อต QR" ในหน้าสะสมแต้ม (owner/manager): สร้างล็อต (ชื่อ/แต้ม/จำนวน ≤2000/วันหมดอายุ) · 🖨️ แผ่น A4 กริด **5×7 ดวงละ 40×40mm** (`qrStickerSheetHtml` ใน `lib/print.ts`) · ⬇ CSV `code,human,points,url` **+ BOM** (Excel อ่านไทยไม่เพี้ยน) · เปิดใช้/ยกเลิก
- US-51 **ลิงก์ใน QR ประกอบที่ server** (`GET /admin/loyalty/batches/:id/codes` คืน `url` มาเลย) เพราะ `liffId` อยู่กับแบรนด์และหน้า line-config เปิดให้แค่ owner — manager ที่สั่งพิมพ์ดึงเองไม่ได้ · ⚠️ **ไม่มี LIFF ID = ปุ่มพิมพ์ปฏิเสธพร้อมบอกวิธีแก้** (พิมพ์ 500 ใบที่สแกนไม่ได้ = ความเสียหายจริง) · `qrcode` โหลด dynamic import ตอนกดพิมพ์ (chunk 10KB gz)
- ✅ ทดสอบครบวง US-51: พิมพ์ → **ถอด QR กลับด้วย jsQR ได้ `https://liff.line.me/<liffId>?e=<code>` ตรงกับที่ US-52 รับ** → CSV 12 แถว → เปิดใช้/ยกเลิกล็อต · ยกเลิกล็อตแล้วสแกนไม่ได้ทั้งล็อต (e2e คุม)
- **US-55 กันโกง + รายงานแต้ม** (EP-14, 8 ก.ย. 2026 — **EP-14 เฟส 1 ครบ US-50…55**): กัน 2 ชั้นใน `earn()` — **เพดานสแกน/วัน/ลูกค้า** (migration **0017** `brands.loyaltyDailyEarnCap`, null = ค่าเริ่มต้น 5, ตั้งได้ที่แท็บรายงาน) + **สแกนพลาด 10 ครั้ง/ชม.** (นับใน memory ไม่เขียน DB — กันสคริปต์เดารหัส) ทั้งคู่ตอบ **429 `RATE_LIMITED`**
- ⚠️ US-55 **เช็คเพดานก่อนเข้า transaction** เพื่อให้โค้ดที่ถูกกั้น**ไม่ถูกเผา** (ลูกค้าเอาไปสแกนวันหลังได้ — e2e คุม) · นับวันด้วย `bangkokDayStart` ไม่ใช่วัน UTC (ไม่งั้นรอบวันตัดตอนบ่ายโมงบ้านเรา) · **นับสแกนพลาดเฉพาะ 404** — ใช้ซ้ำ/เพดานเต็มไม่ใช่การเดารหัส ไม่ควรทำให้ลูกค้าปกติโดนกั้น
- US-55 API: `GET /admin/loyalty/report?days=` (owner/manager — สรุปรายวัน/ต่อล็อต/**แต้มค้างในระบบ** = ภาระที่ต้องจ่ายคืน/`detectAnomalies` 4 ใบใน 10 นาที) · `POST /admin/loyalty/customers/:id/adjust` (**owner เท่านั้น** บังคับใส่เหตุผล, หักเกินยอด 422, ลง ledger `adjust` + audit) · `PATCH /admin/loyalty/settings` ตั้งเพดาน · pure `loyalty/guard.ts` 17 tests
- US-55 UI: แท็บ "รายงาน" ในหน้าสะสมแต้ม (+ปุ่มโหลดใหม่ เพราะเป็นหน้าเปิดค้างเฝ้าดู) · หน้าลูกค้า (admin) มีการ์ดแต้ม + ประวัติ + ปุ่มปรับแต้มมือ (owner)
- **US-56/57 แต้มจากออเดอร์ + ใช้แต้มเป็นส่วนลด** (EP-14, 8 ก.ย. 2026 — **EP-14 ครบทุก subtask**): migration **0018** `brands.loyaltyBahtPerPoint` (null = **ปิด ต้อง opt-in** — แต้มคือภาระที่ร้านต้องจ่ายคืน) · ฐานคิดแต้ม = **subtotal − discount ไม่รวมค่าส่ง** (ค่าส่งไปไรเดอร์ · ส่วนลดจากแต้มไม่วนกลับมาเป็นแต้ม) ปัดลง · pure `pointsForOrder`
- ⚠️ US-56 **ให้แต้มใน transaction เดียวกับการเปลี่ยนสถานะ ไม่ใช่ fire-and-forget** (ต่างจาก push LINE) — "ส่งของแล้วแต้มหาย" คือเรื่องที่ลูกค้าทวงจริง · idempotent ด้วยการเช็ค ledger ที่อ้าง orderId เดิม · Flex แจ้งแต้มผ่านคิวเดิม (`NotifyKind` += `points_earned`, dedupe `points:<orderId>`) และ **อ่านแต้มจาก ledger ไม่คำนวณซ้ำ**
- ⚠️ US-57 LIFF ส่งได้แค่ `loyaltyRewardId` — **ยอดส่วนลดคิดจาก DB ฝั่ง server** (กติกาเหล็ก #2) · ส่วนลดถูก cap ที่ค่าอาหาร ไม่ไปลดค่าส่ง · ตัดแต้ม+ใบแลก(confirmed ผูก orderId)+ledger อยู่ใน transaction เดียวกับการสร้างออเดอร์ → **`orders.create` ถูกห่อ `$transaction` แล้ว** (เดิมเป็น create เดี่ยว) · รางวัล `free_item` ใช้เป็นส่วนลดตอนสั่งไม่ได้ (400) ต้องรับที่ร้าน · audience rule **`points_min`** ยิง broadcast ชวนคนมีแต้มค้างมาแลก
- **US-58 สมุดที่อยู่ + โปรไฟล์ลูกค้า** (EP-15, 8 ก.ย. 2026 — backend ครบ, UI รอ US-59): `addresses` ตารางเดียวเก็บ 2 บทบาทแยกด้วย **`isSaved`** — `false` = snapshot ของออเดอร์ (เดิม) · `true` = หมุดในสมุด · **ออเดอร์ชี้ snapshot เสมอ ไม่ชี้หมุดในสมุด** → ลูกค้าแก้/ลบหมุดทีหลัง ที่อยู่บนออเดอร์เก่าและใบไรเดอร์ไม่เปลี่ยน (มี e2e คุมข้อนี้ผ่าน KDS) · migration 0015 เติม `brandId` (tenant key — เดิมไม่มี ผิดกติกาเหล็ก #1) backfill จาก customer · **สูงสุด 5 หมุด/ลูกค้า** (`MAX_SAVED_ADDRESSES`) เกิน = 422 `ADDRESS_LIMIT` · default ได้ 1 อัน ลบ default = เลื่อนหมุดที่แก้ล่าสุดขึ้นแทน · ลบ = soft delete (`deletedAt`)
- US-58 API (customer JWT, `customerId`/`brandId` มาจาก JWT เท่านั้น): `GET/PATCH /me/profile` (โปรไฟล์+เบอร์+opt-out ในคำขอเดียว, คืน `hasPhone`/`phoneLast4` **ไม่คืนเบอร์เต็ม/phoneEnc**) · `GET/POST/PATCH/DELETE /me/addresses` — ทุกตัวคืน `{addresses:[...]}` ทั้งชุด (POST คืน `created` เพิ่ม) ให้ LIFF แทน state รอบเดียว · `POST /orders` รับ `savedAddressId` **หรือ** `deliveryAddress` (ไม่ส่งเลย = 400, หมุดคนอื่น/ถูกลบ = **404 ไม่ใช่ 403**) + `saveAddress:true` บันทึกหมุดใหม่เข้าสมุด (สมุดเต็ม = ข้ามเงียบ ๆ ออเดอร์ไม่ล้ม)
- US-58 gotchas: **หมุดจากสมุดก็ต้องเช็คเขตซ้ำ** (ครัวย้าย/รัศมีเปลี่ยนได้หลังบันทึก) · `delivery.service` เพิ่ม `reasonCode` (`store_closed`/`out_of_radius`/`no_fee_rule`) + `isDeliverable()` — สมุดที่อยู่ต้องแยก "ส่งไม่ถึง" ออกจาก "ร้านปิดตอนนี้" ไม่งั้นตอนร้านปิดหมุดทุกอันขึ้นนอกเขตพร้อมกัน (`quote()` ตอนสร้างออเดอร์ยังเช็คเวลาทำการเหมือนเดิม) · ป้าย "นอกเขต" **คำนวณสด ไม่เก็บ DB** · pure `profile/address-book.ts` (เพดาน/label/sort/promote default/normalize เบอร์ไทย) 18 tests
- ⚠️ gotcha e2e: `uuid()` ใน `test/e2e.mjs` = `e2e-<timestamp>-<rand>` — **ห้าม `.slice(0,8)`** ตัวหน้าซ้ำกันทุกรอบ (dev-login upsert ตามชื่อ → เจอลูกค้าเก่าที่มีหมุดค้าง เทสต์เพดานพังตั้งแต่รอบ 2) ใช้ทั้งก้อน
- US-19 margin/คุ้มทุน: pure `reports/margin.ts` — `summarizeMargin` (แยกค่าส่งออกจาก contribution เพราะจ่ายไรเดอร์ต่อ · **`costCoverage`** % กล่องที่มีข้อมูลต้นทุน) · `computeBreakeven` (กล่อง/วัน = fixedCostDaily ÷ contributionPerBox, ปัดขึ้น · คืน `null`+`reason` เมื่อยังไม่ตั้งค่าคงที่/ยังไม่กรอกต้นทุน/contribution ≤ 0 — **ไม่เดาตัวเลข**) · `marginByMenu` · 19 tests · API `GET /admin/reports/margin` + `PATCH /admin/reports/fixed-cost` (owner) · UI: `MarginCard.tsx` ใน Dashboard (แถบคืบหน้าเทียบจุดคุ้มทุน + แบนเนอร์เตือนเมื่อ coverage < 100%) + หน้า Menu มีช่องต้นทุน + คอลัมน์กำไร + live preview เตือนต้นทุน > ราคาขาย · ⚠️ `getPublicMenu` ใช้ `select` ชัดเจน **ห้ามคืน costPrice ให้ลูกค้า**
- SEC-1 encrypt LINE creds at-rest (EP-13): `src/common/crypto.ts` AES-256-GCM (`encryptSecret`/`decryptSecret`, รูปแบบ `v1:iv:tag:ct` base64) · คีย์ env `ENCRYPTION_KEY` (hash→32B) · ไม่มีคีย์ (dev/CI) = passthrough · backward-compat: ค่า plaintext เดิม (ไม่มี prefix v1:) อ่านออกตรงๆ · wire ที่ line-config.service (encrypt เขียน) + line.client.config (decrypt อ่าน) · main.ts เตือนถ้า prod ไม่ตั้งคีย์ · ⚠️ prod ต้องตั้ง ENCRYPTION_KEY + ห้ามเปลี่ยนหลังมีข้อมูล (ถอดไม่ออก) · unit 7 tests (round-trip/tamper/legacy)

**Guards** (`src/common/guards/`): `JwtAuthGuard` (customer Bearer JWT — ผูก orders) · `AdminJwtGuard` (admin Bearer JWT, secret แยก `ADMIN_JWT_SECRET`, fail-fast ถ้าไม่ตั้ง) + `RolesGuard` (`@Roles`) ป้องกัน `/api/admin/*` — **ห้าม**ใช้ customer JWT ป้องกัน endpoint แอดมิน · ทุก endpoint admin ที่รับ brandId ต้องผ่าน `assertBrandAccess(admin, brandId)` (กันข้ามแบรนด์) · `AdminKeyGuard` เลิกใช้แล้ว (แทนด้วย admin login US-29)
- Roles: `owner` (จัดการ user + ทุกอย่าง) · `manager` (ร้าน/เมนู/ออเดอร์ ทุกแบรนด์) · `staff` (เฉพาะแบรนด์ที่ผูกใน admin_brands) · owner admin จาก seed: env `ADMIN_SEED_EMAIL`/`ADMIN_SEED_PASSWORD`

## Commands
```bash
npm install                 # ครั้งแรก
npm run db:up               # Postgres(PostGIS) + Redis ผ่าน docker compose
npm run build               # build ทุก workspace (shared ต้องมาก่อนเสมอ)
npm run dev:api             # NestJS watch mode (port 3000, prefix /api)
npm run dev:liff            # Vite dev (port 5173)
npm run dev:admin           # Vite dev (port 5174)
npm test -w apps/api        # unit tests (jest) — โดเมน logic: geo/fee/pricing
npm run test:e2e -w apps/api # E2E smoke (ยิง HTTP จริง — ต้องมี API รันที่ :3000 + DB seed แล้ว)
npm run prisma:migrate -w apps/api   # apply migration (ต้องมี DB)
npm run prisma:seed -w apps/api      # seed แบรนด์ชิมชีวา + เมนู + เขตส่งตัวอย่าง
```

**CI** (`.github/workflows/ci.yml`): 2 งาน — `build` (npm ci + build + jest ทุก workspace) และ `e2e` (postgis+redis service → migrate deploy → seed → boot API → `test/e2e.mjs` 197 เช็ค). E2E ครอบกติกาเหล็ก: คิดเงิน server, เช็คเขต 422, idempotent, tenant isolation, RBAC (customer JWT เข้า admin ไม่ได้), status transition, พักร้าน→422. ไม่พึ่ง lib นอก (fetch ล้วน).

**CD** (`.github/workflows/deploy.yml`): GHCR image + pull — trigger `workflow_run` หลัง CI ผ่านบน `main` (หรือ `workflow_dispatch`) → build+push `aroihoh-api`+`aroihoh-static` ขึ้น GHCR → SSH VPS รัน `scripts/deploy.sh` (ดูหัวข้อ CD ด้านบน + `docs/deploy-hetzner.md` / `docs/cd-secrets-checklist.md`). deploy แบบ manual เดิมยังทำได้ (compose คง `build:` ไว้).

## ข้อห้าม / กติกาเหล็ก
1. **ทุก query DB ต้องกรอง tenant key** (merchant_id/brand_id) ตั้งแต่ migration แรก — ห้ามมีตาราง business data ที่ไม่มี tenant key
2. **เงินคำนวณฝั่ง server เท่านั้น** — ค่าส่ง, ส่วนลด, ยอดรวม ห้ามเชื่อค่าจาก client (client แสดงผลอย่างเดียว)
3. **LINE webhook ต้อง verify `x-line-signature` ทุก event** ก่อนประมวลผล
4. **สร้างออเดอร์/รับ payment webhook ต้อง idempotent** — กันกดซ้ำ/ยิงซ้ำ (US-04, US-06)
5. **เช็คเขตจัดส่ง server-side ซ้ำเสมอตอนยืนยันออเดอร์** — ผล check ฝั่ง LIFF เป็นแค่ UX (422 ถ้านอกเขต)
6. **PDPA**: ห้าม log PII (ชื่อ/เบอร์/พิกัดลูกค้า) ลง log ทั่วไป, broadcast ต้องเคารพ opt-out
7. Push message ผ่านคิว BullMQ + กันส่งซ้ำด้วย `message_logs` (โควตา LINE มีจำกัด)

## การตัดสินใจที่ยังค้าง (ห้ามเดาเอง — ดู task กลุ่ม PRE-DEV)
- **SETUP-4**: payment gateway (Omise/2C2P/GB Prime Pay) — บล็อก US-06 PromptPay
- **ADR-02** (ค้างแต่ตัดสินโดยพฤตินัยแล้ว): ship **Haversine radius** (`delivery/geo.ts`) ส่วน PostGIS ยังไม่ได้ใช้จริงแม้เปิด extension + เก็บ geometry ไว้แล้ว — ถ้าจะปิด ADR ให้เขียนยืนยันทางที่ใช้อยู่

## การตัดสินใจที่เคาะแล้ว
- ADR-01: **NestJS** (ไม่ใช่ Express เปล่า) — โครงสร้าง module ชัด เหมาะกับ solo dev + AI ทำงานหลาย session
- ADR-04: **monorepo npm workspaces** (เครื่อง dev ไม่มี pnpm) — shared types อยู่ `packages/shared`
- DB: PostgreSQL + **PostGIS** (docker image `postgis/postgis:16-3.4`) + Prisma
- **ADR-0005 tenant schema** (`docs/adr/0005-tenant-schema.md`, Accepted): **Merchant → Brand → Kitchen** — Brand = tenant key ของ app (`brandId`) · Kitchen = จุดผลิต+เขตส่ง+กฎค่าส่ง แชร์หลายแบรนด์ผ่าน `BrandKitchen` (m:n) · เฟส A = 1 Merchant, N Brand, 1 Kitchen · **`schema.prisma` เขียนครบแล้ว 20 models** (ไม่ใช่ไฟล์ว่างอีกต่อไป)
- ADR-03 hosting: **Hetzner Cloud (CX22) + Docker + Caddy** ที่มีอยู่เดิม โดเมน `jivecode.click` — โค้ด/compose/Caddyfile พร้อมแล้ว (`docs/deploy-hetzner.md`) **deploy แล้ว** ที่ `49.13.57.24` (`/opt/aroihoh`) — `aroihoh-api/order/admin.jivecode.click` HTTPS ผ่าน Caddy บน host
- ADR-05: multi-tenant **SaaS-first** (isolate ด้วย merchantId) + dedicated-instance เป็นทางเลือกลูกค้าใหญ่
- ADR-06: LINE OA = **1 Brand : 1 OA** (creds เก็บที่ Brand) — defer LineChannel/shared-OA จนกว่าจะมีดีมานด์
- ADR-07: **House of Brands** (1 แบรนด์ 1 OA แยก identity) + backend รวม
- ⚠️ ADR-05/06/07 ปิดใน PM แล้วแต่**ยังไม่มีไฟล์ใน `docs/adr/`** (มีแค่ 0005) — เนื้อหาอยู่ใน Knowledge Board

## Conventions
- TypeScript strict ทุก workspace
- Branch: `main` = deployable, feature branch ตั้งชื่อตามรหัส story เช่น `us-01-liff-login`
- Commit message อ้างรหัส story/task เช่น `US-06: add promptpay webhook`
- เก็บเงินเป็นหน่วยย่อย (สตางค์/integer) ไม่ใช้ float
