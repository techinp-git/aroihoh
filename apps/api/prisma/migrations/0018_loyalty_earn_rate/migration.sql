-- US-56: ให้แต้มอัตโนมัติเมื่อออเดอร์ delivery ส่งสำเร็จ
-- "ทุกกี่บาทได้ 1 แต้ม" — null = ปิดฟีเจอร์ (ไม่ให้แต้มอัตโนมัติ) ซึ่งเป็นค่าตั้งต้น
-- ตั้งใจให้ต้อง opt-in เพราะแต้มคือภาระที่ร้านต้องจ่ายคืน ไม่ควรเปิดให้เองโดยเจ้าของไม่รู้
ALTER TABLE "brands" ADD COLUMN "loyaltyBahtPerPoint" INTEGER;
