-- US-55: เพดานสแกนต่อวันของลูกค้า 1 คน ตั้งได้ต่อแบรนด์
-- เคสที่กันจริง: พนักงานเก็บสติกเกอร์ที่ยังไม่แจกมาสแกนเข้าบัญชีตัวเองรวดเดียว
-- null = ใช้ค่าเริ่มต้นในโค้ด (DEFAULT_DAILY_EARN_CAP) — ร้านส่วนใหญ่ไม่ต้องตั้งเอง
ALTER TABLE "brands" ADD COLUMN "loyaltyDailyEarnCap" INTEGER;
